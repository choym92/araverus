# Python dependencies for finance pipeline scripts
# Install: pip install -r scripts/requirements.txt

# HTTP client
httpx>=0.27.0

# Supabase client
supabase>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# ML/NLP dependencies
sentence-transformers>=2.2.0  # Embedding-based ranking + BAAI/bge-base-en-v1.5
numpy>=1.24.0
vecs>=0.4.0  # pgvector Python client (optional, for direct vector ops)

# Content extraction (for crawl_article.py)
trafilatura>=1.6.0
lxml>=4.9.0
newspaper4k>=0.9.0  # Fast extraction with metadata (hybrid approach)

# Web crawling (for crawl_article.py)
crawl4ai>=0.4.0

# OpenAI API (for llm_analysis.py)
openai>=1.0.0

# Google AI (for generate_briefing.py)
google-genai>=1.0.0
google-cloud-texttospeech>=2.16.0

# Whisper (for sentence-level alignment in generate_briefing.py)
openai-whisper>=20231117

# Postgres direct connection (for db_query.py)
psycopg2-binary>=2.9.0
